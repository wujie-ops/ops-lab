# <一句话说清干了什么>

> **使用说明**：复制本文件为 `notes-YYYY-MM-DD-简短主题.md`，然后逐节填写。
> 不需要的节可以删（比如没有失败过程就删掉"过程/失败"相关小节）。
> 带 🟡 标记的是**最容易漏、但最有价值**的部分，别跳。

- **日期**：YYYY-MM-DD
- **耗时**：约 _ 小时
- **结果**：✅ 成功 / ❌ 未解决 / 🟡 绕过（临时方案）
- **宿主 / 环境**：<!-- 例：Ubuntu Server 22.04.5 / KVM -->

---

## 一句话结论

<!-- 🟡 最重要的一行。让半年后的你和面试官 10 秒看懂结论。
     写法：<做了什么> + <关键动作/参数> + <为什么这样做>
     例：无图形环境下不要用 `virt-install --cdrom`；应用 `--location` 并显式
         指定 kernel/initrd，因为 Ubuntu live-server ISO 是 casper 布局。 -->

---

## 目的

<!-- 一句话：为什么做这件事。 -->

---

## 环境

<!-- 只写"影响结果的关键信息"，不是写作文。通常 3-6 行。
     必须包含：系统版本、相关组件/版本、可以决定成败的前提条件 -->

| 项 | 值 |
|---|---|
| 系统 | |
| 关键组件与版本 | |
| 前提条件 | |

---

## 过程

> 每次尝试都按「**预期 → 实际 → 根因 → 结论**」四段写。
> 🟡 **只写"报错了"等于没写**——根因才是这份记录的价值所在。

### 尝试 1：<做了什么>（✅/❌）

```bash
# 完整命令，能直接复制的那种
```

- **预期**：
- **实际**：
- **根因**：
- **结论**：

### 尝试 2：<做了什么>（✅/❌）

```bash
```

- **预期**：
- **实际**：
- **根因**：
- **结论**：

---

## 可用方案

<!-- 把最终成功的命令完整写在这里，标注"已验证" -->

```bash
```

---

## 验证

> 🟡 **别跳过这一节**。"完成了"必须是**被证明的**，不是你说的。
> 贴真实输出，不要只写"已成功"。

```bash
# 逐条给出验证命令与预期结果
```

- [ ] 验证项 1：命令 + 结果
- [ ] 验证项 2：命令 + 结果

---

## 关键结论

<!-- 抽象的、可迁移的规则。表格让"不做会怎样"一目了然 -->

| # | 结论 | 可迁移性 |
|---|---|---|
| 1 | | 通用 / 仅本环境 |
| 2 | | |

---

## 反思：我走了哪些弯路

<!-- 🟡 这一节面试时最值钱。写"我一开始以为…后来发现…"
     以及"下次应该先做什么"。这是方法论，不是流水账。 -->

- 我一开始以为……，实际上……
  → **下次应当先……**

---

## 待办 / 下一步

<!-- 未完成的部分明确标出来，别假装完整 -->

- [ ]
- [ ]

---

## 参考

- `man xxx`
- <文档链接>

---

<!-- ==================== 提交前自检 ====================
[ ] 开头有结果标记 + 一句话结论
[ ] 命令完整可复制，不是片段
[ ] 每次失败都写了根因，不只是"报错了"
[ ] 有"验证"一节，且给出实际输出
[ ] 结论是"可迁移的道理"，不是"换个命令就好了"
[ ] 有"待办"，未完成的部分如实标出
[ ] 文件名符合规范：notes-YYYY-MM-DD-主题.md
[ ] 没有真实密码、密钥、公网 IP、token
[ ] 通读一遍，改掉错别字
==================================================== -->
